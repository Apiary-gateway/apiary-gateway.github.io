{
  "name": "apiary-case-study",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pre-deploy": "rm -rf docs && mkdir docs && npm run build && cp -r dist/* docs/ && touch docs/.nojekyll"
  },
  "dependencies": {
    "astro": "^5.7.4"
  }
}
